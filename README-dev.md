🧪 Developer Mode Workflow (most likely in dev)
rm -rf pub/static/frontend/*
rm -rf var/view_preprocessed/*
Clear cache:
php bin/magento cache:flush
Force browser reload (hard refresh or clear cache):

